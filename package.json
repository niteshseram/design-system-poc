{
  "name": "root",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "build:tokens": "yarn workspace @auzmor/design-tokens build",
    "lerna:publish": "lerna publish from-package"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
